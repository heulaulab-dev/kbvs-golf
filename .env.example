# ── Golfie — .env template ──────────────────────────────────────────
# Copy to .env.development / .env.staging / .env.production
# and fill in real values. All keys are required.

# App
APP_NAME=Golfie
APP_VERSION=1.0.0

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# API base URLs
TOURNAMENT_API_BASE_URL=
NEWS_API_BASE_URL=
CADDY_API_BASE_URL=
